---
id: DAILY-2026-06-17
aliases: ["DAILY-2026-06-17"]
type: daily
title: Session du 2026-06-17
tags: []
source: ""
linkedTo: []
relevantFor: []
tier: 1
created: 2026-06-17
updated: 2026-06-17
---

# 📅 Session du 2026-06-17

> Journal de session in-repo (continuité inter-sessions). Complète la mémoire harness `MEMORY.md`.

## 🎯 Objet de la session
Rendre **MASTAURIGE collaboratif temps réel** (100 % de l'état partagé entre traitants) + régler l'hébergement (PC Admin sans Python).

## ✅ Ce qui a avancé
- **Hébergement** : PC Admin sans Python → **Python portable embarqué** (`SERVEUR_COLLABORATIF\python\`), `.bat` l'utilise en priorité + **relance auto**. (⚠ non testable sur le poste dev qui bloque les .exe non approuvés ; test réel sur le PC Admin.)
- **Phase 1 — serveur** réécrit : sert l'instance **7BB** (copiée dans `mastaurige\`), magasin central du « mod layer », **fusion collections par id**, **SSE temps réel**, **persistance + sauvegardes horodatées + restauration**. Testé ✅.
- **Phase 2 — `collab.js`** : amorce localStorage depuis le serveur, enveloppe setItem/removeItem → POST, EventSource `/flux` → applique + refresh (jamais pendant une saisie), badge connectés. `mast-traitant` reste local. Injecté dans index_master + melmil_ili. `node --check` ✅, **chaîne SSE prouvée** ✅.
- **Correctifs terrain** : (a) **crash-loop** du `.bat` (priorité au portable refusé par la sécu) → ordre py système→portable, test `-c pass`, garde anti-boucle `choice` ; (b) **comptage des connectés faux** (cliquer MELMIL = +1) → id **par fenêtre** (`sessionStorage`), `presence()` compte les id distincts, battement 5 s ; (c) **lenteur de chargement** (même sur poste puissant) → cause = `localhost`→IPv6 vs serveur IPv4-only (**2033 ms→29 ms**) réglée par **double pile `ServeurDualStack`** + keep-alive HTTP/1.1 + TCP_NODELAY/buffering + cache 304 + table MIME fixe. Port configurable (`MAST_PORT`).

## 🧩 Décisions / leçons (→ notes atomiques créées)
- [[ARCH-008]] — architecture MASTAURIGE collaboratif temps réel (serveur central + `collab.js`), pivot = **réutilisation du modèle Export/Import** (`isModKey`/`COLLECTION_KEYS`/`mergeCollection`).
- [[LESSON-013]] — serveur HTTP Python local lent : double pile IPv6, Nagle, cache 304 (leçon perf réutilisable).
- [[TOOL-011]] — `generer_melmil.py` (import MELMIL unifié GESTIM + JEMM) — outil livré la veille, documenté.

## 📝 Fichiers autoritaires modifiés (la vérité, pas le vault)
- `MASTAURIGE/MEMOIRE.md` (§ collaboratif). Outillage : `D:\CECPC\MASTAURIGE\SERVEUR_COLLABORATIF\` (serveur.py, collab.js, .bat, mastaurige\ = copie 7BB).

## ⏭️ Prochaine étape (reprise next session)
- 🧪 **Valider en NAVIGATEUR sur 2 machines** (amorce, écriture↔écriture temps réel, reload, badge) — prérequis : navigateurs traitants qui joignent le port du PC Admin.
- **Phase 3** : rafraîchissement fin sans reload (re-rendu ciblé cards/MELMIL). **Phase 4** : présence « qui édite quoi » + verrou par card + restauration sauvegarde + doc.
- (Toujours en attente) test du serveur sur le PC Admin relié au serveur d'exercice.

## 🔗 Notes liées
- Reprise depuis [[DAILY-2026-06-16]].
- [[ARCH-008]] · [[LESSON-013]] · [[TOOL-011]] · [[PROJ-MASTAURIGE]] · [[PROJ-AURIGE-7BB]]
