# JOURNAL — MASTORION (historique chronologique, append-only)

> Compte-rendus datés des travaux sur la plateforme MASTORION. Les règles et l'état durable sont dans `MEMOIRE.md`.

---

## 2026-07-27 — Création de l'agent + première analyse du repo

- Agent MASTORION créé (21ᵉ agent) selon la checklist CLAUDE.md : registre, compteur NOYAU (20→21), branche ROUTAGE, prompt `SYSTEME\PROMPTS\mastorion.md`, dossier `MASTORION\` (README/MEMOIRE/JOURNAL — convention 2 fichiers dès le jour 1).
- Contexte donné par l'utilisateur : MASTORION = plateforme parallèle à MASTAURIGE, **beaucoup plus performante**, visant les exercices **division/corps**. Le repo `D:\CECPC\MASTORION\mastorion-v0` est un clone GitHub ; le travail passe par MINERVE pour capitaliser le savoir existant et améliorer ensuite la plateforme.
- **Consigne ferme** : aucune modification du dossier `D:\CECPC\MASTORION` pour le moment (lecture seule).
- Première analyse complète du repo effectuée en lecture seule — synthèse consignée dans `MEMOIRE.md` § « Connaissance de la plateforme » ; **rapport intégral** (références `fichier:ligne`) archivé dans `ANALYSE_REPO_2026-07-27.md`.
- Convention de branches posée par l'utilisateur : dans `mastorion-v0`, travail sur la branche **`MEYTRE`** (créée via GitHub Desktop), jamais sur `main` ; MINERVE reste sur `main`. Règle consignée dans `MEMOIRE.md` § Règles.
- Procédure de test local établie (lecture seule du code : `db.ts`, `index.ts` ensureAdmin, `docker-compose.yml`, `.env.prod.example`) : `.env.example` manquant, provider mysql en dur, admin auto-créé. Consignée dans `MEMOIRE.md` § « Procédure de test local ».

## 2026-07-27 (suite) — Mise en route du test local (autorisée par l'utilisateur)

- L'utilisateur a levé la consigne lecture seule pour les **fichiers non versionnés** (installation/test). Docker Desktop installé (étape 0 utilisateur) → blocage WSL absent → `wsl --install --no-distribution` + reboot (utilisateur) → moteur Docker 29.6.2 OK.
- Conteneur `mastorion-db` créé (mariadb:11, port 3306, identifiants `mastorion`, restart unless-stopped).
- `apps/api/.env` créé (mysql localhost, admin `admin@mastorion.local` / `admin123`, JWT local) — vérifié ignoré par git.
- **Découverte bloquante : D: en exFAT** → symlinks npm workspaces impossibles (2 échecs EISDIR reproductibles) → **clone d'exécution créé sur `C:\CECPC\MASTORION\mastorion-v0`** (NTFS, branche MEYTRE, origin→GitHub, .env copié) ; règle consignée en MEMOIRE.
- ✅ **PLATEFORME LANCÉE ET VALIDÉE** : `npm install` OK sur C:, `npm run dev` → API :3000 (v0.2.49) + social :4200 + admin :4201 + cockpit :4202 tous en HTTP 200 ; login admin OK ; `seed:full` exécuté (⚠ découvert : il RESET la base → compte unique `test@test.fr` / `password123`). Premier test utilisateur imminent.
- **Clarifications de périmètre demandées par l'utilisateur** (fin de session) : « où est MASSTALK ? » → l'Admin le remplace intégralement (vestiges : `masstalkUsername`, CLI reconcile) · « ou plutôt RocketChat » → **vérifié par grep : aucune messagerie dans la plateforme**, 0 occurrence dans le repo, rien dans le schéma Prisma → consigné en § « Ce que la plateforme NE fait PAS » + roadmap n°10 (canal messagerie fictive, à clarifier : outil externe CECPC ou brique à concevoir) · « déploiement serveur ? » → confirmé, § « Modèle de déploiement cible ».

## 2026-07-28 (suite) — Audit de la classification par les 4 agents-experts

- Déclencheur utilisateur : `@6e_Army`/`@20e_Army` en MER - POLITICIEN. Correction de la règle (groupe « officiel » = statut ≠ métier → croiser `activite`).
- 4 agents saisis en parallèle, chacun lisant sa mémoire avant de juger : **ANALYSTE** (Mercure), **ANALYSTE_ARN**, **ANALYSTE_BOT** (+France+transverses), **MINAUTORE+GUILLAUME** (emploi éditorial réel dans les injects). ~70 corrections appliquées dans `OVERRIDES`.
- **2 bugs de parsing découverts grâce à l'audit** (l'AIEA signalée absente par ANALYSTE_BOT) : séparateur de la 1ʳᵉ fiche + pseudos à apostrophe/espaces → **29 fiches récupérées, 220 → 249 personas**.
- Points doctrinaux : règle des comptes à façade (nature > masque), anti-inversion Bothnia (Tikhanov/Saniki = opposition PRO-MER), acteurs économiques ARN sortis de « patriote », milice TANTALE en groupe clandestin.
- Résultat intermédiaire : 49 groupes, 0 non-classé. 4 arbitrages soumis à l'utilisateur.
- **Arbitrages tranchés le même jour** : (1) Titane ramené sous MER, code TIT supprimé ; (2) pas de région française réelle → `@clambroise55` en ARN ; (3) `@GavrilovBorislav` = sergent MER d'après sa bio (⚠ contredit la revue 7BB du 21/06 — injects à revoir) ; (4) homonyme `@Kozi_Aus` exclu. Classeur final : **248 personas, 47 groupes**.

## 2026-07-28 (suite) — Annuaire visuel de lecture (demande utilisateur)

- Nouveau classeur `RECAPS\ANNUAIRE_PERSONAS.xlsx` : rendu esthétique, filtres natifs, code couleur camp/pays, tableau de bord avec graphiques — pour la lecture humaine, hors format d'import. Généré par `OUTILS\generer_annuaire_visuel.py`, qui réutilise `generer_bibliotheque.construire()` (source unique, cohérence garantie avec le fichier d'import).
- 🐛 Bug trouvé en le construisant : le champ `pays` des personas transverses (ONG, institutions, animation) valait l'artefact CASW brut `"Animation"` au lieu de rester vide — ce champ alimente le profil importé dans MASTORION. Corrigé dans `generer_bibliotheque.py` ; les deux classeurs régénérés et vérifiés (AIEA/Amnesty/CICR ont bien `pays` vide désormais).

## 2026-07-28 — Refonte de la taxonomie des groupes

- Constat utilisateur après import dans MASTORION : **55 groupes** hérités de CASW, souvent à 1 membre, libellés à rallonge. Demande : repenser les groupes en `PAYS - FONCTION` (MER - POLITICIEN, ARN - JOURNALISTE, ARN - PACIFISTE…), « tout détailler pour que ce soit très bien rangé ».
- Bug corrigé dans `generer_bibliotheque.py` : `charger_casw()` capturait les puces de la **section biographie** comme des groupes → restreint à la section `### Groupes`.
- Nouvelle taxonomie implémentée (tables `PAYS_CODE`, `MAP_CASW`, `MAP_EHO`, `MAP_ACTIVITE`, `MOTIFS_INSTITUTION`, `OVERRIDES`) + **cumul de groupes** par persona. **55 → 35 groupes**, 6 singletons légitimes, 2,2 groupes/persona, 0 « NON CLASSE ».
- 4 pièges identifiés et documentés en MEMOIRE (regex `\bun\b`/`\beu\b`, `\d{2}` = faux département alors que la Lorraine H-préfixe du 7BB est arnlandaise, pays « ANIMATION » masquant les institutions, personas multi-groupes).
- Classeur régénéré ; README bibliothèques mis à jour avec le tableau pays × fonctions.

- 📊 **Récap des injects joués généré** (demande utilisateur) : `RECAPS\RECAP_INJECTS_GUILLAUME_MINOTAURE.xlsx` — onglets GUILLAUME (70 injects/176 produits) et MINOTAURE (84/105), avec types de produit. Script `OUTILS\generer_recap_injects.py` + `RECAPS\README.md`. Mise au point : lecteur JS tolérant nécessaire (commentaires internes + **clés non quotées côté 2BB**) ; sources de produits différentes par exercice (index typé 2BB vs `articles_data.js` 7BB). 3 écarts de source documentés (22 injects 2BB hors MELMIL, 4 injects sans produit, 9 HTML 7BB non déclarés).
- ⭐ **1ʳᵉ bibliothèque de personas générée** (demande utilisateur) : `BIBLIOTHEQUES\BIBLIOTHEQUE_TEST_3_EXERCICES.xlsx` — 220 personas (MINOTAURE 35 + GUILLAUME 13 + ORION 222, dédoublonnés), 3 onglets = 3 camps, priorité bio MINOTAURE > GUILLAUME > ORION. Script `OUTILS\generer_bibliotheque.py` + `BIBLIOTHEQUES\README.md`. Sources en lecture seule (avatars.js 7BB/2BB, bios.js EHO, registre MASTAURIGE, CASW ORION). 4 itérations de mise au point : heuristique du registre captait des libellés d'injects → filtres ajoutés ; commentaires d'`avatars.js` d'abord sur-appliqués (en-tête et séparateurs `====` pris pour des descriptions) → capture restreinte à l'intérieur du tableau + seuil de lettres. Validé : 0 doublon, colonnes conformes à l'import.
- **Outillage sauvegarde/restauration créé** (question utilisateur « peut-on repartir sur une base vierge ? ») : dossier `C:\CECPC\MASTORION\SAUVEGARDES\` avec `BASE_VIERGE.sql` (état de référence figé : 1 user, 0 groupe/scénario/post), `SAUVEGARDER.bat` et `RESTAURER.bat` (base + images, confirmation « OUI »). Mécanisme **testé** : groupe témoin inséré → restauration → disparu, user conservé. Analyse au passage de `resetDb()` : ne supprime pas la table `groups`.
- **Audit de la mémoire de l'agent** (demande utilisateur : « ces notions sont-elles bien assimilées ? ») → 1 règle devenue fausse corrigée (§ périmètre d'écriture : la lecture seule stricte ne vaut plus que pour D:, l'exécution est autorisée sur C:, le code source reste sous autorisation) + 3 manques comblés (MASSTALK↔Admin, absence de messagerie, roadmap n°10) + 1 ligne obsolète supprimée (« Sentinel hors périmètre »).

- ✅ **SENTINEL MONTÉ** (demande utilisateur) : découverte que turbo dev lance déjà sentinel-api (:3100) + sentinel-ui (:4203, repli de port silencieux) ; modèles Ollama natifs pullés (qwen2.5:3b + snowflake-arctic-embed:137m) ; conteneurs sentinel-pg (pgvector, :5433) + sentinel-worker buildés/démarrés via compose overlay ; bug résolu : secret JWT divergent entre api et sentinel-api (« Token invalide ») → `.env` aligné sur `mastorion-dev-secret` + redémarrage dev ; validation : `/api/sentinel-health` = status ok, 8 tables `sentinel_*` créées par le worker dans pgvector. Détails durables en MEMOIRE.

## 2026-09-09 — Réseau RZO + 54 fiches EHO manquantes → bibliothèque 248 → 404 personas

- Suite du portage EHO éditable côté MASTAURIGE (vierge) : l'utilisateur demande de vérifier si les comptes nouvellement découverts (réseau RZO 107 acteurs, section addendum) existent dans `BIBLIOTHEQUE_TEST_3_EXERCICES.xlsx`. Vérifié précisément : addendum déjà couvert (0 manquant, source commune `avatars.js`) ; RZO totalement absent (`generer_bibliotheque.py` ne lisait jamais `rzo_data.js`).
- **Découverte plus large** : 54 des 59 fiches `bios.js` (EHO) n'avaient jamais été importées — tout le gouvernement/commandement militaire/clergé des deux pays (présidents, ministres, préfets, maires, généraux, évêques) — car `construire()` ne créait un persona qu'à partir d'un compte **avec handle** (avatars.js/CASW), l'EHO ne servant qu'à enrichir une bio déjà repérée. Beaucoup de RZO recoupent ces 54 (ex. Nadia Promesy = même personne côté RZO et côté EHO).
- Question posée à l'utilisateur (3 options) → **intégration complète sans doublon** choisie.
- Implémentation `generer_bibliotheque.py` : `construire()` étendu (ajoute les fiches EHO sans handle, `@<id_eho>` synthétique) + nouvelle fonction `fusionner_rzo()` (fusion par nom si déjà présent — tag `RESEAU RZO` + `GROUPE CLANDESTIN` si réseau HFM/NOM/Redskulls — sinon fiche légère créée) + classificateurs `classer_rzo_fonction`/`classer_rzo_pays`.
- 🐛 2 bugs trouvés et corrigés en vérifiant : (1) « Pro-MER » classé à tort comme nationalité mercurienne (corrigé en `ARN - PRO-MERCURE`, cohérent avec l'audit à 4 agents) ; (2) `norm()` vide silencieusement un nom 100 % cyrillique → 3 acteurs RZO (Алексей Аксёненко/Сюзанна Светличная/Капитан Хэдок) fusionnés à tort en une seule fiche (corrigé par repli sur le nom brut minuscule quand `norm()` renvoie vide — piège à retenir pour tout futur générateur).
- **Résultat vérifié : 404 personas, 0 doublon username/email**, cas de fusion contrôlés un par un (Promesy/Adriane/Danevois/Martin : bio complète conservée, tag réseau ajouté, aucun doublon). `generer_annuaire_visuel.py` aligné (appelle aussi `fusionner_rzo()`).

## 2026-09-09 (suite, soirée) — PHASE 1 EHO v2 (Fable) : socle joueurs/animateur + purge des overrides morts

- ⭐ **Mission EHO v2 lancée** (prompt `MASTORION\PROMPTS\prompt_fable_eho_v2.md`, plan approuvé « ok pour tout ») : modèles d'EHO (SKOLKAN PERSONA / VIERGE), double vue animateur/joueurs, fiches et placements joueurs comparables sans JAMAIS toucher les fiches officielles. Arbitrages actés : EHO **par cellule** (équipe d'1 possible) · rôles `EHO_ANIM`/`EHO_PLAYER` (colonne `role` libre → 0 migration) · comptes joueurs = comptes MASTORION créés par l'anim · EHO joueur dépouillé (nom+handle+portrait seulement).
- **Phase 1 livrée (commit `b2e91ec` sur `feat/eho`)** : 4 tables **strictement additives** dans `apps/api/prisma/schema/eho.prisma` (`eho_state`, `eho_teams`, `eho_team_members`, `eho_assessments`) — preuve faite : 129 colonnes préexistantes inchangées, données intactes. Références vers `users` par entier **sans relation Prisma** (la relation imposerait le champ inverse sur `User` → retouche interdite de base.prisma). Module `/api/eho` : state, cellules+joueurs (anim), `my/board`+`my/assessments` (joueur), `compare/:teamId` (anim, écarts pays/camp).
- **Critère structurel humain/persona** : un persona = compte `users` **sans aucun rôle** (`user_roles`). Vérifié : 452 sans rôle, humains avec. C'est ce qui rend les comptes humains intouchables par les opérations d'exercice (et le futur « appliquer un modèle »).
- ⚠ **Email joueurs en `@eho.mastorion.local`** — un `@mastorion.local` pourrait fusionner en silence avec un persona à un futur import (piège Gavrilov).
- **Étanchéité vérifiée bout en bout** (cellule test « CELLULE ALPHA » + `joueur_alpha_1`/`alpha123`, conservés pour les phases suivantes) : board joueur sans AUCUN champ officiel · 403 sur endpoints anim/admin · 404 sur fiche visant un humain · fiche officielle relue inchangée après écritures joueur · comparaison anim OK (écart pays détecté).
- 🐛 **`OVERRIDES` : 12 clés en double dans un même literal Python** (dernier gagne, EN SILENCE). 17 entrées mortes purgées — sortie prouvée identique (empreinte `282430728bc919bf`, 452 lignes avant/après) — + **garde-fou `verifier_overrides_uniques()`** (échec bruyant au prochain doublon, testé). Les couches `OVERRIDES.update({...})` d'audit restent légitimes (vérif par bloc).
- ⏳ **ARBITRAGE EN ATTENTE — Analyste Mercure** : `@GavrilovBorislav` = `MER - MILITAIRE` par arbitrage utilisateur du 2026-07-28 (« sa bio fait foi : sergent 47e division »), qui contredit la revue MINOTAURE 06-21 (diaspora HDieuze, cf. ANALYSTE\ARNLAND). Depuis la scission en 2 comptes, la question se repose : `@The_Grass_hopper` (ORION) porte naturellement l'identité sergent — `@GavrilovBorislav` (AURIGE) devrait-il redevenir ARN diaspora ? L'utilisateur a dit « l'Analyste Mercure tranchera » — ne rien changer d'ici là.

## 2026-09-09 (nuit) — PHASE 2 EHO v2 : modèles d'EHO + application verrouillée

- **Phase 2 livrée (commit `bf97889` sur `feat/eho`)** : écran `/modeles` = **nouvelle première page** de l'app (cartes SKOLKAN PERSONA / VIERGE + emplacement « à venir », bandeau d'état, bande 4 couleurs pays). Navigation croisée modèles ↔ trombinoscope.
- **Catalogue = donnée** : un modèle = un dossier `apps/api/data/eho/templates/<code>/` (manifest + payload au format import), **gitignoré** (contenu d'exercice ≠ code). `POST /api/eho/templates` **capture la base courante** comme nouveau modèle → créer un modèle futur = opération de contenu, zéro code. SKOLKAN-PERSONA créé ainsi (452 personas / 56 groupes).
- **Application verrouillée à 5 niveaux, tous côté serveur** : rôle anim · `confirm=<code>` tapé · **sauvegarde JSON auto avant toute suppression** (`data/eho/backups/`, format import-compatible + fiches joueurs) · suppression limitée aux comptes **sans rôle** · uploads jamais supprimés (les `avatar_url` restent valides d'une application à l'autre).
- ✅ **Aller-retour RÉEL vérifié sur la base** : sans confirm → 400 · VIERGE → 0/0, admin ET joueur se connectent toujours · SKOLKAN-PERSONA → 452/56, **112 portraits servis en 200**, mentions multi-comptes restaurées à l'identique. ⚠ à savoir : l'application régénère les **ids** des personas → les fiches joueurs sont réinitialisées par conception (dans la sauvegarde).
- ⏭ Reste : phase 3 (mode joueur — EHO dépouillé, drag & drop, rédaction de fiches) puis phase 4 (vue comparative anim) et phase 5 (gestion/CRUD complet + cellules).

## 2026-09-09 (nuit, suite) — PHASE 3 EHO v2 : mode joueur

- **Phase 3 livrée (commit `30a2721` sur `feat/eho`)** : page `/mon-eho` (rôle `EHO_PLAYER`) — bac « À CLASSER » avec les personas **dépouillés** (le serveur n'envoie ni pays, ni fonction, ni camp, ni bio), **glisser-déposer** vers MERCURE / ARNLAND / BOTHNIA / FRANCE / AUTRE-INTERNATIONAL (placement optimiste, la carte REVIENT si le serveur refuse), fiche d'analyse de cellule au clic (pays+camp+fonction estimés, texte libre, trace « dernière analyse : X »), anneau du portrait coloré au camp estimé.
- **Connexion par rôle** : l'app accepte désormais APP_ADMIN / EHO_ANIM / EHO_PLAYER ; atterrissage `accueil()` (anim → `/modeles`, joueur → `/mon-eho`) ; gardes croisés (un joueur tapant `/modeles` est ramené chez lui). L'UI ne fait que router — l'API re-vérifie tout.
- **Accessibilité clavier** : équivalent complet du drag & drop — Entrée sur une carte ouvre la fiche où le pays se choisit en liste.
- `@angular/cdk` 21.2.5 ajouté **au seul workspace eho**.
- ⚠ Piège vécu : après un gros ajout, le watcher `ng serve` n'avait PAS rechargé la nouvelle route (bundle servi sans `mon-eho` alors que `ng build` passait) → **toujours vérifier le bundle servi** (`grep` de la route dans `main.js`), redémarrer le serveur au besoin.
- ⏭ Reste : phase 4 (vue comparative anim) puis phase 5 (gestion/CRUD + cellules dans l'UI).

## 2026-09-09 (nuit, fin) — PHASE 4 EHO v2 : vue comparative animateur

- **Phase 4 livrée (commit `82678de` sur `feat/eho`)** : page `/comparaison` (animateur) + endpoint agrégat `GET /api/eho/compare` (couverture et taux d'erreur par cellule, top 20 des personas mal lus toutes cellules).
- **Lecture d'un coup d'œil** : tuiles par cellule (évalués/452, fiches, écarts pays/camp en rouge) · bandeau « personas les plus mal lus » · détail par persona en 2 colonnes OFFICIEL (froid, bio dépliable) / CELLULE (chaud, verdicts ✓/✗ champ par champ, fiche d'analyse, auteur) · lignes en écart bordées rouge et triées en tête · filtre « que les écarts » · camps en pastilles 🔴🔵⚪.
- Vérifié sur données réelles : CELLULE ALPHA 3 évalués → 2 écarts pays (Gavrilov, Peters) + 1 écart camp (Peters) détectés, la lecture correcte (HmunikVoice) hors du bandeau « mal lus ». 403 joueur re-testé côté API en phase 1.
- Serveur eho redémarré systématiquement après gros ajout (piège du watcher consigné en phase 3) — bundle vérifié.
- ⏭ Reste : **phase 5** — gestion complète (CRUD personas, cellules & joueurs dans l'UI, import/export) + déplacement des mentions « COMPTES MULTIPLES » de `bio` (publique côté réseau social) vers `observations`.

## 2026-09-10 — PHASE 5 EHO v2 : l'EHO propriétaire des personas, l'Admin recentré sur les humains

- **Phase 5 livrée (commits `22d46fa` eho + `ca848d6` admin, poussés)** — 2 consignes utilisateur intégrées : « l'Admin ne configure que les comptes joueurs/animateurs/admins, plus de personas mélangés » et « l'import/export Excel passe de l'Admin à l'EHO ».
- **Module `/api/eho/personas`** (animateur) : CRUD complet **en un appel** (identité+fiche+groupes), groupes, portrait, suppression avec dépendances, **import à garde anti-fusion** — upsert par USERNAME seul ; ligne visant un **compte humain** → refusée ; **email/masto_id déjà porté par une autre fiche** → refusée ligne à ligne avec message (leçon Gavrilov codée en dur dans l'API). Le service front n'appelle **plus aucun** `/api/admin/*` → un `EHO_ANIM` sans APP_ADMIN est pleinement fonctionnel.
- **Page `/gestion`** (remplace `/avatars`) : onglets Personas (table paginée + fiche complète) · Cellules & joueurs (création des comptes, mdp affichés pour distribution) · Import/Export (xlsx parsé côté client — un onglet « CAMP … » vaut appartenance au camp ; export réimportable tel quel).
- **Admin recentré (retouches minimales sur l'existant)** : paramètre **optionnel** `comptes=humains|personas` sur `GET /api/admin/users` (défaut = comportement historique inchangé, vérifié 454/2) ; le front admin demande `comptes=humains` ; boutons Importer/Exporter masqués (mécanique conservée). **Aucun changement de schéma.**
- 🔒 **Fuite corrigée** : mentions « COMPTES MULTIPLES » déplacées de la **bio (publique côté réseau social)** vers **`observations`** (animateur seul) — générateur MINERVE corrigé (+ purge auto des bios héritées), classeur + annuaire régénérés (4/4 vérifiés), **base vivante assainie via la nouvelle API**, modèle SKOLKAN-PERSONA **recapturé** depuis la base propre.
- ✅ Vérifié 8/8 : admin humains seuls (2) · admin sans filtre inchangé (454) · EHO 452 · import refuse @test (humain) · import refuse collision email · maj légitime par username · board joueur intact · 403 joueur sur /personas.
- **MISSION EHO v2 : les 5 phases sont livrées.** Restent des pistes (non demandées) : rôle EHO_ANIM sans APP_ADMIN à créer depuis l'UI, portraits dans les payloads de modèles pour portabilité inter-machines, champ camp officiel dédié.

## 2026-09-09 — Onboarding dév MASTAURIGE→MASTORION : branche `feat/eho` + intégration des commits Xavier

- Mission utilisateur : onboarder le développeur MASTAURIGE sur l'intégration dans MASTORION — pull `main`, créer `feat/eho`, monter la stack avec des données, fournir identifiants/URL, relire le fichier d'import. **Consigne forte : « SOIS LE MOINS DESTRUCTIF POSSIBLE SUR LE SCHEMA ».**
- Branche `feat/eho` créée sur le clone d'exécution `C:\CECPC\MASTORION\mastorion-v0`.
- L'utilisateur intègre ensuite 2 commits de Xavier dans `MEYTRE` → demande de vérifier la cohérence. **GitHub injoignable** (`git fetch origin` en timeout à 2 min) → contourné en ajoutant un remote **local** `dlocal` → `D:/CECPC/MASTORION/mastorion-v0` (le clone GitHub Desktop), fetch OK. *Astuce à réutiliser quand le réseau/l'auth GitHub manque : les deux clones se synchronisent entre eux.*
- Contenu des 2 commits (`af24bfc` doc Docusaurus + `076190b` stack de prod Traefik) : 48 fichiers, +27 753/−8 018, dont `package-lock.json` (33 806 lignes) et une **nouvelle app `apps/docs`** dans le turborepo.
- **Contrôle du schéma (le point sensible) : 0 fichier `*prisma*`/`*migration*`/`*.sql` touché** par les 2 commits ; 17 modèles au schéma = 17 tables en base ; données intactes (404 personas / 54 groupes). Consigne respectée.
- `feat/eho` alignée sur `076190b` par `reset --hard dlocal/main` (aucun commit propre, arbre vérifié propre au préalable) → `npm install` (nouvelles dépendances Docusaurus, exit 0) → stack relancée → **6 services UP** (3000, 3100, 4200, 4201, 4202, 4203), login `test@test.fr` OK (`APP_ADMIN`), `/api/admin/users` et `/api/admin/groups` servent bien les données importées.
- 🔑 **Accès GitHub établi en fin de séance** (demande utilisateur « tu vas te connecter à Git ça sera plus simple »). Mon diagnostic initial « GitHub injoignable » était **faux** — github.com répond en 0,28 s ; le blocage venait de Git Credential Manager en attente de saisie, impossible dans mon shell (ni TTY ni GUI, + `GIT_TERMINAL_PROMPT=0`/`GCM_INTERACTIVE=never` imposés par le harnais → *device code* échouant sur `/dev/tty`, exit 128). Résolu en ouvrant une **fenêtre PowerShell sur le bureau de l'utilisateur** avec ces deux variables neutralisées (1er essai raté : `Start-Process` hérite de l'environnement du parent) → validation navigateur par l'utilisateur → identifiants en cache Windows. **`feat/eho` poussée sur GitHub** (droits d'écriture confirmés sur le dépôt de Xavier) ; `MEYTRE` du clone C: avancée à `076190b` (0 commit propre). Procédure complète en MEMOIRE.
- ⭐ **APP `apps/eho` CRÉÉE dans le turborepo** (commit `87801e0`, poussé sur `origin/feat/eho`). ⚠ **Erreur de ma part corrigée** : j'avais bloqué TOUT le chantier sur « les consignes du développeur » alors que la demande spécifiait déjà le livrable (« créer une app au sein du turborepo, essentiellement de l'admin pour créer des avatars ») — l'utilisateur a dû me le rappeler. Leçon : distinguer ce qui est **spécifié** (à faire) de ce qui est **à préciser** (à demander).
- **Architecture de l'app EHO** : Angular 21 standalone + PrimeNG/Aura, calquée fichier par fichier sur `apps/admin` (angular.json, tsconfig, proxy.conf.json, guard APP_ADMIN, intercepteur). **Port 4204** (4200 web · 4201 admin · 4202 cockpit · 4203 docs). Clés de session propres `eho_token`/`eho_user` — chaque front étant sur une origine distincte en dev, les sessions ne se partagent pas (piège vécu avec le cockpit).
- 🔑 **Découverte structurante : AUCUNE migration nécessaire.** Le modèle `User` porte déjà tous les champs du trombinoscope MASTAURIGE (`age`, `genre`, `pays`, `label`, `origine`, `religion`, `situation`, `caractere`, `langage`, `activite`, `bio`, `observations`, `qualifications`, `aime`, `deteste`, `avatar_url`). L'app se contente donc de consommer les endpoints existants — **0 fichier de schéma touché**, consigne de non-destruction intégralement respectée.
- ⚠ **Contrat de l'API à connaître** : `POST /api/admin/users` n'accepte **que l'identité** (`username`, `email`, `password`, `display_name`) ; les champs de fiche EHO doivent être posés par un **`PATCH /api/admin/users/:id`** ensuite, et les groupes par `PATCH /api/admin/users/:id/groups`. La création se fait donc en 2 (voire 3) appels — comportement de l'API existante, pas une limitation de l'app.
- Vérifié : `ng build` exit 0 sans erreur ni warning, app servie sur :4204, proxy `/api` fonctionnel (login `APP_ADMIN` OK depuis 4204). Reste à câbler (documenté dans `apps/eho/README.md`) : import direct des classeurs MASTAURIGE, édition d'un persona existant, curseur d'orientation rouge/bleu (à mapper sur un champ existant — **arbitrer avant tout ajout de colonne**).
- 🧹 **DOUBLONS RÉSORBÉS + COMPTES MULTIPLES RENDUS EXPLICITES** (décision utilisateur : « je n'aime pas les doublons » ; « si une personne possède plusieurs comptes, il faut que ce soit clairement inscrit dans la fiche bio pour qu'on ne se loupe pas ; tout doit être clair, fonctionnel et fluide » ; l'arbitrage de fond sur Gavrilov revient à l'**Analyste Mercure**, au vu des exercices).
- **Consultation de l'Analyste (règle CLAUDE.md respectée)** : `ANALYSTE\ARNLAND\MEMOIRE.md` documente **@GavrilovBorislav = « Borislav Gavrilov — habitant HDieuze, diaspora mercurianophone d'Arnland » 🔴**, tandis que la fiche CASW ORION 26 (id 1589) donne **@The_Grass_hopper = « héros de guerre mercurien », militaire, 25 ans**. Conclusion : **une personne, deux comptes, deux exercices** — pas un doublon à supprimer.
- 🐛 **FUSION SILENCIEUSE À L'IMPORT — piège majeur découvert** : le classeur contenait 2 lignes Gavrilov, la base n'en avait qu'**une**. L'upsert MASTORION remonte par `masto_id` puis par `email` : les deux lignes partageant `gavrilov.borislav@icloud.com`, **@GavrilovBorislav (pourtant joué dans les injects AURIGE) avait été absorbé sans le moindre avertissement**. ⚠ **Règle : deux fiches distinctes DOIVENT avoir `email` et `masto_id` distincts, sinon l'import en perd une en silence.**
- **Correctifs dans `generer_bibliotheque.py`** : (1) `cle_personne()` — clé d'identité ignorant les **titres/grades** (`Dr`, `MG`, `Gén.`, `Sgt`…), les accents et l'ordre des mots, appliquée aux 3 points de fusion (EHO sans handle, RZO, planche) ; (2) table **`ALIAS_PERSONNES`** explicite et auditable pour les variantes indéduisibles (`Rémi`/`Rémy` Laffin) — jamais de rapprochement flou automatique, qui confondrait des homonymes ; (3) `ascii_id()` — plus aucun `username` accentué (`@arn_rémy_laffin` corrigé) ; (4) **`clarifier_comptes_multiples()`** — rend `email`/`masto_id` uniques ET **écrit la mention en tête des deux bios** : « ⚠ COMPTES MULTIPLES — cette entité possède aussi le compte @X ».
- **Résultat** : 456 → **452 personas au classeur** (4 doublons fusionnés : Nele Meyer, Rémi/Rémy Laffin, Gén. Brrouette, Gén. Palmquetil — survivants vérifiés un par un avant suppression), **5 fiches obsolètes supprimées en base**, **453 en base** (452 + admin), **112 portraits**, **0 username non-ASCII, 0 email ou masto_id en double**. Les 2 seuls cas de comptes multiples subsistants (**Gavrilov** et le média **Arnish Times**) sont volontaires et annotés dans les 4 bios.
- ⚠ **Régression que j'ai introduite puis rattrapée** : en unifiant les identifiants j'ai d'abord renommé les 49 personas de la planche (`@jonasjunker` → `@jonas_junker`), ce qui aurait créé 49 doublons et orphelin leurs portraits. **Le schéma de `username` déjà en base fait foi — ne jamais le changer sans migration.** Retour à `norm()` pour cette source. De même, `rzo-x.jpg` (gabarit vide) s'était glissé sur une fiche par une voie d'appariement que le filtre ne couvrait pas → portrait retiré.
- 🔴 **TROU DE COUVERTURE MAJEUR CORRIGÉ — 49 acteurs de la planche EHO manquaient (dont TOUS les politiques)** (signalé par l'utilisateur : « les politiciens de cette page ne sont pas dans mastorion »). **Cause racine** : `generer_bibliotheque.py` lisait uniquement les **fichiers de données** (`avatars.js`, `bios.js`, `rzo_data.js`, CASW) ; or `ACTEURS_A3_model.html` contient **65 cartes écrites EN DUR dans le HTML** — gouvernements complets (présidents, ministres), états-majors (CHOD, CGS, chefs SOF/renseignement), justice, acteurs internationaux, entités para-étatiques — qui n'existent dans **aucun** fichier de données. 16 seulement étaient présents par recoupement. ⚠ **Leçon transposable : une planche EHO n'est pas qu'un rendu de ses sources — elle porte du contenu propre. Toujours auditer le HTML lui-même, pas seulement les .js qui l'alimentent.**
- **Correctif** : nouvelle source `A7_PLANCHE` + `charger_planche()` (parse les cartes statiques avec leur contexte page/section) + `classer_planche()` + `fusionner_planche()` (même principe que `fusionner_rzo` : fusion par nom si déjà présent, création sinon). Résultat : **404 → 456 personas** (52 créés, 13 fusionnés), **65/65 acteurs de la planche désormais présents — 0 manquant** (vérifié par recomparaison). Nouvelle fonction **`JUSTICE`** (Cour suprême / juges ARN). `generer_annuaire_visuel.py` aligné.
- 🐛 **Bug de classification trouvé et corrigé en cours de route — le même piège que le 2026-07-28** : mes mots-clés étaient cherchés en **sous-chaîne**, donc « politiq**ue** » contenait `"ue "` → **les 10 politiques de Mercure classés « INSTITUTION INTERNATIONALE »**, et `N.O.M.` matchait sur « anti-**OTAN** ». Corrigé par une recherche à **limites de mots** (`\b…\b`) + un ordre de priorité explicite (clandestin → média → justice → international → opposition → militaire → politicien). **Réflexe à garder : dans ce corpus francophone, tout mot-clé court DOIT être délimité.**
- 🖼️ **PORTRAITS — 8 de plus après la correction : 103 → 111.** Les portraits des ministres (Junker, Stoph, Ribiki), des généraux (PRUNIÈRE, ZHUKOV), du Sgt VORIN, de Guterres et de Rutte n'étaient pas rattachables tant que les personas n'existaient pas. Total : **111 photos sur 456 personas** (Arnland 94 · Mercure 13 · Bothnia 4).
- 🖼️ **PORTRAITS DU TROMBINOSCOPE INTÉGRÉS — 103 personas sur 404** (demande utilisateur). Source : `D:\CECPC\MASTAURIGE\LOCALSTORAGE_WEB_VERSION\Sites\Trombinoscope\img\` (113 images exploitables : 30 en racine + 83 dans `img/rzo/`, hors 3 drapeaux). Méthode : téléversement par `POST /api/admin/users/upload-avatar` puis `PATCH /api/admin/users/:id` avec l'`avatar_url` renvoyée — **aucun accès direct au disque ni à la base, aucun changement de schéma**. Répartition : Arnland 92 · Mercure 7 · Bothnia 4. Vérifié : image servie en HTTP 200 (`image/jpeg`, 29 Ko).
- **Appariement en 3 passes** (script `apparier2.py`, exécuté d'abord en simulation) : (1) **nom de fichier == `username`** — c'est la passe la plus productive, tous les `rzo-*.jpg` et `arn_*.jpg` portent le handle ; (2) préfixe pays (`MER_`/`ARN_`/`BOT_`) + handle ; (3) nom affiché normalisé (sans accents, **sans grades** MG/BG/Sgt/Col…, **mots triés** pour absorber « Nom Prénom » ↔ « Prénom Nom »). Passer de la v1 à la v2 a gagné 97 → 102 grâce à deux correctifs : le **découpage camelCase** (`GavrilovBorislav` → `Gavrilov Borislav`) et la comparaison directe au username. +1 arbitrage manuel (`MER_MakarovSidorovskiy.jpg` → id 102 `@SidorovskyMakarov`, ambigu avec `@MakarovSid`) = **103**.
- ⚠ **7 personnalités ont une photo mais N'EXISTENT PAS en base** — à créer si on les veut : **António Guterres**, **Mark Rutte**, **Sgt Aleksander VORIN**, les 3 ministres Mercure **Jonas Junker** (Défense), **Dominic Stoph** (Aff. étrangères), **Voichek Ribiki** (Intérieur), et les généraux **MG Thierry PRUNIÈRE** (41 DIV) / **MG Mikhail ZHUKOV** (43 DIV). Elles figurent dans la planche `ACTEURS_A3_model.html` mais pas dans les sources qui ont alimenté la bibliothèque (`avatars.js`/`bios.js`/CASW/RZO) — piste : ce sont des figures **2BB/GUILLAUME** (cf. mémoire `todo_ministres_mercure.md`). Les 4 autres non-appariées sont sans enjeu : doublon `BOT_Presidente_Lena Peters.jpg` (version avec espace), gabarit `rzo-x.jpg`, drapeaux.
- 🐛 **2 pièges découverts et documentés en MEMOIRE** : (1) conflit de port `apps/docs` (4203 explicite) ↔ `sentinel-ui` (repli silencieux sur 4203) — contourné **sans modifier le code de Xavier** via `npx turbo dev --filter='!docs'` ; (2) `prisma migrate status` **inutilisable** (P3019 : `migration_lock.toml` = `sqlite` vs schéma `mysql`, défaut **préexistant** au commit initial `b7c6332`) — Prisma suggère de supprimer l'historique de migrations, **ne jamais le faire ici**.
